/**
 * shared/config — 디자인 토큰 단일 진입점(RN).
 * Tailwind 유틸리티는 tailwind.config.js 의 @zipboda/tokens preset으로 이미 적용됨.
 * StyleSheet 등에서 원시 토큰 값이 필요하면 이 모듈(@zipboda/tokens의 ES6 export)을 사용.
 */
export * from "@zipboda/tokens";
